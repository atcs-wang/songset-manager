<%# Must pass in:
{ profilelist: [{categoryId, categoryName}],
  searchquery
}
%>

<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/stdhead') %>
</head>

<body>

    <%- include('../partials/navbar') %>

    <!-- Actual page content! -->
    <div class="container">
        <h2>All Users:</h2>

            <div class="section" id="controls">
                <form method="get">
                    <div class="row center-align">
                        <div class="input-field col s9">
                          <i class="material-icons prefix">search</i>
                          <input id="searchBar" type="text" name="search" class="validate" value="<%=searchquery%>">
                          <label for="searchBar">Search: (User Id, Username, Prilege...)</label>
                        </div>
                    </div>
                </form>
            </div>
    
            <div class="section" id="songlist">
                <table class="sortable">
                    <thead>
                        <tr>
                            <th>User ID</th>
                            <th>Username</th>
                            <th>Privilege</th>
                            <th class="no-sort"></th>
                        </tr>
                
                    </thead>
                    <tbody>
                        <% for (profile of profilelist) { %>
                        <tr>
                            <td><%= profile.user_id %></td>
                            <td><%= profile.username %></td>
                            <td><%= profile.privilege%></td>
                            <td>
                                <a class="btn-small waves-effect waves-light" href="/user/<%= profile.user_id %>">
                                    <i class="material-icons right">edit</i>
                                    Edit
                                </a>
                                
                                <form class="confirm-submit right" method="post" display="inline" action="user/<%=profile.user_id%>"
                                    data-confirm-msg='Confirm that you want to delete this user by retyping "<%=profile.username%>":' 
                                    data-require-retype="<%=profile.username%>">
                                    
                                    <input type="hidden" name="method" value="delete">
                                    <!-- <input type="hidden" name="user_id" value="<%=profile.user_id%>"> -->
                        
                                    <button class="btn waves-effect waves-light red" type="submit">
                                        <i class="material-icons right">delete</i>
                                        Delete User
                                    </button>
                                </form>

                            </td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>

        </div>

        <%- include('../partials/stdfoot') %>

</body>

</html>